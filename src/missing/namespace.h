/*
 * Stub for NetBSD's libc-internal namespace.h, needed only to satisfy the
 * unconditional #include "namespace.h" in the vendored regcomp.c/regexec.c/
 * regerror.c/regfree.c (see REGEX-COPYRIGHT). It aliases libc-internal names
 * to their public ones inside NetBSD's own libc build; outside of that build
 * there is nothing to do.
 */
